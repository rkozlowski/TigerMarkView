<Project>

  <!-- Build policy applies repository-wide. Product/release metadata does not: the four shipped
       projects import Version.props explicitly, while tests remain ordinary test assemblies. -->
  <PropertyGroup>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

</Project>
